# Supabase (Project Settings → API) — the anon key is safe to expose client-side
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Wherever the backend from /backend ends up running
VITE_BACKEND_URL=http://localhost:3000
